---
const today = new Date();
---

<footer
  class="py-12"
  style="background-color: var(--bg-dark); color: var(--text-on-dark);"
>
  <div class="max-w-7xl mx-auto px-6 text-center">
    <p class="mb-4" style="color: var(--text-secondary);">
      © {today.getFullYear()} 东波哥. 用心记录，用技术创造价值.
    </p>
    <div class="flex justify-center space-x-6 mb-4">
      <a
        href="/about"
        class="footer-link transition-colors duration-200"
        style="color: var(--text-secondary);">关于</a
      >
      <a
        href="/contact"
        class="footer-link transition-colors duration-200"
        style="color: var(--text-secondary);">联系</a
      >
      <a
        href="/rss.xml"
        class="footer-link transition-colors duration-200"
        style="color: var(--text-secondary);">RSS</a
      >
    </div>
    <div class="pt-4" style="border-top: 1px solid var(--border-color-dark);">
      <p class="text-sm" style="color: var(--text-secondary);">
        <a
          href="https://beian.miit.gov.cn/"
          target="_blank"
          class="footer-link transition-colors duration-200"
        >
          粤ICP备2021046742号-1
        </a>
      </p>
    </div>
  </div>
</footer>

<style>
  /* CSS 变量定义 */
  :root {
    /* 固定背景色 */
    --bg-light: #ffffff;
    --bg-dark: #101827;
    --bg-logo: #1f1a12;

    /* 文字颜色 */
    --text-on-light: #1f2937;
    --text-on-dark: #f9fafb;
    --text-secondary: #6b7280;

    /* 交互与品牌色 */
    --action-blue: #0b69a3;
    --action-gold: #fbbf24;

    /* 状态反馈 */
    --alert-success: #16a34a;
    --alert-error: #dc2626;

    /* 其他 */
    --border-color-light: #e5e7eb;
    --border-color-dark: #374151;
    --transition-speed: 0.3s;
  }

  /* Footer 链接悬停效果 */
  .footer-link:hover {
    color: var(--action-gold) !important;
  }
</style>
